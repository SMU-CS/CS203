/**
 * An interface for EventDetails
 * @returns {interface} 
 */
export interface EventDetailsType {
    id: string;
    title: string;
    startDate: string;
    endDate: string;
    location: string;
    imagePic: string;
}